# Trainual API token, used by `npm run build` to read page content.
# Trainual -> Settings -> API. Content read scope is sufficient.
TRAINUAL_API_TOKEN=


# GitHub Pages target for `npm run deploy:gh` (must be a PUBLIC repo on the free plan).
GH_PAGES_REPO=
